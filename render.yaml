services:
  - type: web
    name: bio-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bio_api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
